<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
    <body>
        <div><h2><ins>Posts:</ins></h2> <br>
            {% for post in posts %}
                <h2>{{ post.title }}</h2>
                <h2>{{ post.content }}</h2>
                <h2>{{ post.resume }}</h2>
                <hr>
            {% endfor %}
        </div>
    </body>
</html>